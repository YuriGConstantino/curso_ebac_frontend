"use strict";
const campoNome = document.getElementById('nome');
const form = document.getElementsByTagName('form');
const botao = document.getElementById('btn');
botao === null || botao === void 0 ? void 0 : botao.addEventListener('click', (e) => {
});
